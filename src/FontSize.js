






var FontSize = function (baseValue, adjustment, unit, size, ratio) {
  return baseValue * adjustment * Math.pow(ratio, size);
}


var LineHeightAdjustment = function (baseValue, unit, size, ratio, fontSizeAdjustment, lineHeightAdjustment) {
  var fontSize baseValue * adjustment * Math.pow(ratio, size);
}